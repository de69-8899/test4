# Provider mode: auto | local | external
# auto = uses advanced external APIs when keys are present, else local fallback
CONVERTER_PROVIDER=auto
COMPRESSION_PROVIDER=auto
BG_REMOVAL_PROVIDER=auto

# Advanced external APIs
CLOUDCONVERT_API_KEY=
TINYPNG_API_KEY=
REMOVEBG_API_KEY=

# Optional enterprise PDF compression endpoint
ADVANCED_PDF_COMPRESS_API_URL=
ADVANCED_PDF_COMPRESS_API_KEY=
