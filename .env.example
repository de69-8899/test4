# Provider mode: external | local
# external (default) = use requested advanced API providers first
CONVERTER_PROVIDER=external
COMPRESSION_PROVIDER=external
BG_REMOVAL_PROVIDER=external

# Requested API credentials
CLOUDCONVERT_API_KEY=
TINYPNG_API_KEY=
REMOVEBG_API_KEY=
